import React from "react"
import Layout from "../components/layout"

const FourPage = ({data}) => {
  return(
  <Layout pageTitle="Page not found" pageDescription="Page not found">
 <p>Page not found. <a href="/"> Return to home page.</a></p>
 </Layout>
 )
}

export default FourPage
